export class User {

  $key: number;
  email: string;
  password: {
    pwd: string;
    confirmPwd: string;
  };

  phone: string;
  terms: boolean;

  constructor(values: Object = {}) {
    //Constructor initialization
    Object.assign(this, values);
  }

}
